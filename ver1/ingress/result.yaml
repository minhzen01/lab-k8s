apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: result-ingress
  namespace: client
spec:
  rules:
  - http:
      paths:
      - path: /client
        pathType: Prefix
        backend:
          service:
            name: result-service
            port:
              number: 81
